# Generated by Cargo
# 自 portreaper-core 拆分起，workspace 根在仓库根，产物落在 /target/（根
# .gitignore 覆盖）。这条规则**仍要留着**：拆分前的旧检出里 src-tauri/target/
# 可能还堆着几 GB 遗留产物，而 oxfmt 靠 gitignore 决定扫描范围 —— 删掉它，
# `vp check` 会一头扎进 target 里去格式化 cargo 的 .json 指纹文件（实测踩到）。
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas
